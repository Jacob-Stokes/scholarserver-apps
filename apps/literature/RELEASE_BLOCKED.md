# Literature — draft only, release blocked

No approved image, container build, native startup, browser API, packaged UI,
provisioning, installer or actual Gateway proof exists. Development manifests are
intentionally incomplete and unusable. Never add these to catalog discovery.

Release needs reviewed immutable native AMD64/ARM64 images and dependency notices,
an instance-specific credential provisioning/rotation path, actual authenticated
Gateway discovery/calls, duplicate-instance namespace/network/credential checks,
and an operator-wide arXiv rate policy (across all machines, not per instance).
The current in-process gate is sufficient only for one isolated connector process;
no deployment or multi-instance compliance is claimed. UI is a source-only setup
preview with no settings save or connection test. Schema/Compose conformance,
same-origin Manager serving and negative unauthorised HTTP tests remain required.

Only mocked metadata tests are intended in this pass. No full-text downloads,
library writes, engine installs, upstream accounts or paid services are authorised.
