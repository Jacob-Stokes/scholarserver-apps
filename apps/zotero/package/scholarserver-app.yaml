schemaVersion: 1
id: org.scholarserver.zotero
name: Zotero
packageVersion: 0.2.2
compatibility:
  platform: ">=0.1.0"
  schema: 1
upstream:
  name: Zotero
  version: 10.0.1
  license: AGPL-3.0-or-later
support:
  tier: official
  architectures: [amd64, arm64]
delivery:
  class: remote-desktop
  management: managed
tenancy:
  mode: per-workspace
capabilities:
  provides: [references.zotero-library, attachments.resolve, mcp.zotero]
  requires: [gateway.mcp]
  conflicts: []
resources:
  minimum: { cpu: 1, memory: 2GiB, disk: 5GiB }
  recommended: { cpu: 2, memory: 4GiB, disk: 25GiB }
permissions:
  networkEgress: [sync.zotero.org, api.zotero.org, user-configured-webdav]
  hostMounts: []
  devices: []
hardening:
  readOnlyRoot: true
  noNewPrivileges: true
  dropCapabilities: [ALL]
images:
  - service: desktop
    reference: ghcr.io/jacob-stokes/scholarserver-zotero-desktop@sha256:11b9d406d0411188a1a0c7c92d7cbb605e9d17a1d048b4a06de58dfb8629049a
  - service: controller
    reference: ghcr.io/jacob-stokes/scholarserver-zotero-controller@sha256:19ef933d62e8a88049062dbe9be1524e7bfc5b7b61b582c72438a0beb7b2a617
  - service: mcp
    reference: ghcr.io/jacob-stokes/scholarserver-zotero-mcp@sha256:f94897dd23955c085fc00f25a44bd1d612f321db69ea174f394e46f3fc602d5c
data:
  - id: desktop-config
    mountPath: /config
    owner: zotero
    filesystem: { uid: 10001, gid: 10001, mode: "0700" }
    backup: filesystem-consistent
    retention: preserve
  - id: library
    mountPath: /data
    owner: zotero
    filesystem: { uid: 10001, gid: 10001, mode: "0750" }
    backup: application-consistent
    retention: preserve
    exportFormat: Zotero data directory
  - id: runtime
    mountPath: /runtime
    owner: zotero
    filesystem: { uid: 10001, gid: 10001, mode: "0700" }
    backup: reproducible
    retention: preserve
  - id: linked-files
    mountPath: /linked
    owner: zotero
    filesystem: { uid: 10001, gid: 10001, mode: "0750" }
    backup: filesystem-consistent
    retention: preserve
    exportFormat: plain-directory
endpoints:
  - id: desktop
    service: desktop
    port: 3000
    protocol: http
    exposure: human-optional
    auth: authentik-forward-single
  - id: mcp
    service: desktop
    port: 7012
    protocol: http
    exposure: gateway
    auth: service-identity
    gateway:
      displayName: Zotero
      namespace: zotero
      hostname: desktop
      credentialData: runtime
      credentialFile: service-token
lifecycle:
  supportsDisable: true
  removeDataDefault: false
  rollback: backup-required
onboarding:
  readyWhen: compose-health
  checklist:
    - Connect a Zotero account in ScholarServer
    - Choose how attachments are stored and downloaded
    - Authorize ScholarServer for local library access
    - Run the first synchronization
  actions:
    - id: status
      data: runtime
      timeoutSeconds: 10
      fields: []
    - id: account-start
      data: runtime
      timeoutSeconds: 30
      fields: []
    - id: account-complete
      data: runtime
      timeoutSeconds: 30
      fields: []
    - id: configure-storage
      data: runtime
      timeoutSeconds: 30
      fields:
        - { id: storageMode, type: string, secret: false, required: true }
        - { id: downloadMode, type: string, secret: false, required: true }
        - { id: groupFileSync, type: boolean, secret: false, required: true }
    - id: configure-webdav
      data: runtime
      timeoutSeconds: 180
      fields:
        - { id: url, type: string, secret: false, required: true }
        - { id: username, type: string, secret: false, required: true }
        - { id: password, type: string, secret: true, required: true }
        - { id: downloadMode, type: string, secret: false, required: true }
        - { id: groupFileSync, type: boolean, secret: false, required: true }
    - id: authorize-local
      data: runtime
      timeoutSeconds: 300
      activateGateway: true
      fields: []
    - id: sync-now
      data: runtime
      timeoutSeconds: 900
      fields: []
    - id: resolve-attachment
      data: runtime
      timeoutSeconds: 60
      fields:
        - { id: attachmentKey, type: string, secret: false, required: true }
