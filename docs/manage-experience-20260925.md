# Manage presentation qualification

Native image source: `f85ffe13a4ba2ca5a333c3961dbcad901ed29d70`.
The [public workflow](https://github.com/Jacob-Stokes/scholarserver-apps/actions/runs/36071705265)
passed on native AMD64 and ARM64, including the named native service, restart
and development browser gates. Both published architecture manifests/configs,
source labels and root filesystem identities were compared with their retained
receipts before updating the seven changed image references.

The new `.manage.20260925.1` package candidates include the shared settings-row
presentation, exact Manage return links and optional `ui.configurationPath`.
This field needs compatible Manager and executor schema support. The metadata
follow-up changes immutable references and versions; it does not change the
qualified runtime source. All 19 source-lock records validate. Unchanged images
keep their existing immutable references.

The earlier source pass passed `npm test`, lint and synthetic Chrome app-screen
tests, including completed settings, retained recovery/error feedback, cancelled
and saved attachment edits and direct n8n configuration navigation. These checks
do not establish real account connectivity, successful personal-data updates or
fresh installation. Native development gates retain their existing scoped limits.

Retained-host rollout and authenticated browser acceptance are recorded in the
core deployment map separately. Docling and personal Zotero writable shared-folder
recovery gates remain in force; publishing images does not override them.
Project-vault documentation remains pending; no vault note was written here.
