# Three n8n research reports — 12 September 2026

## Source and artifact

Source checkpoint: `a64ee9def04aacf9a1034a7c3be77f5013f83f6d`, pushed to
`codex/beginner-acceptance` in scholarserver-apps. The catalog has six research
templates. The additions are a weekly reading roundup, a missing-reference-details
check, and a plain Markdown bibliography. See
[workflow scope and prerequisites](../apps/n8n/RESEARCH_WORKFLOWS.md).

Native ARM64 integration candidate:
`sha256:5af4919822e308c0581a75d19a42c79253341ddf62770a5d74e70f49ac64f936`.
Its source fingerprint is
`sha256:00085a9908fc0af6a5609e262b3a4ffd06138385de2402d0ca5d3ba1cd905c12`.
The image labels record the source revision and fingerprint. The source guard
passed before and after the build. The fourteen-layer known-content check passed.

The n8n runtime is unchanged from beta.6:
`ghcr.io/jacob-stokes/scholarserver-n8n@sha256:48c4e461b86201a3b18d5d35dcf5ba9e6250e76fa697df1482871efcb413d68d`.

## Verification

- Full locked source check passed: dependency installation, lint, tests,
  packaging checks and all declared workspace builds.
- Focused tests cover complete UTC weeks, day/year/leap-day boundaries, bounded
  schedules, fixed subfolders, path rejection, empty and issue-free output,
  Markdown escaping and deterministic filenames.
- Mocked Chrome catalog checks passed for all six cards, search, tag/application
  filters, sorting, setup, save failure/draft preservation and mobile layout.
- Native ARM64 read-only containers passed owner/service-identity setup and
  restart, native workflow creation, enable/disable, reload and direct-edit
  protection. Browser installation covered each of the three new reports plus
  reading notes, using distinct installation identities and nested folder roots.
- After restarting n8n and integration, all six templates and a second reading
  copy executed twice. Real Linux create-only note writes produced one dated
  report per new type and preserved a user-edit sentinel on repeat execution.
- Each new report executed against empty input with zero note writes. Each then
  rejected revoked action grants, also with zero note writes. Issue-free audits
  are covered by Code-node unit tests, not a separate native scenario.

The test used real n8n HTTP, Code, schedule configuration and credential storage,
with a synthetic Manager/research-service fixture and real disposable Linux note
files. It did not connect a personal Zotero library, run actual Docling conversion,
test Obsidian device sync, or exercise real Manager/executor action queues in this
pass. Schedule activation/deactivation was checked, not a 24-hour wall-clock wait.

Test scope `scholarserver-n8n-ci-35215` was removed; its containers, three volumes
and network were verified absent. No paid server was created. Existing development
state and unrelated Docker data were preserved.

## Review and release boundary

The retained loopback development stack explicitly selects this local candidate.
Firefox displays `http://127.0.0.1:18320/`, Catalog, with all six cards. The banner
identifies UI HMR separately from the unpublished backend revision. This stack
has no Manager/research-app grants; its availability warning is expected.
UI edits hot-reload; templates and backend edits require a new native candidate.

Local review screenshots are in `.dev/automation-catalog-review/`:
`native-catalog-desktop.png`, `native-catalog-mobile.png`, and
`native-catalog-setup-1.png` through `native-catalog-setup-3.png`. These show the
disposable synthetic-fixture test, not a live personal installation.

No image was published, no official catalog package was signed, and Freelove was
not updated. Package manifests, the accepted image-source lock and core's paired
apps pin remain unchanged. The release lock check correctly rejects the changed
`n8n-app` fingerprint until a new native multi-architecture candidate is qualified
and published. Source push and this local preview are not release acceptance.
