# Vendored ScholarServer UI release

This directory is the application-monorepo snapshot of `@scholarserver/ui@0.1.0`.
The source of truth lives in the ScholarServer platform repository. Release
automation must replace this snapshot from the published package and verify its
checksum before building first-party application interfaces.
